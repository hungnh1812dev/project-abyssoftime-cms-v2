services:
  # ── Go API (Web Service, native Go, free tier) ─────────────────────────────
  # Native Go builds faster than Docker on Render free tier.
  # To switch to Docker: see render.docker.yaml
  - type: web
    name: cms-api
    runtime: go
    plan: free
    rootDir: apps/api
    buildCommand: CGO_ENABLED=0 go build -trimpath -ldflags="-s -w" -o bin/server ./cmd/server
    startCommand: ./bin/server
    envVars:
      - key: PORT
        value: "8080"

      # ── Database (Supabase PostgreSQL) ─────────────────────────────────────
      - key: DB_DRIVER
        value: "postgres"
      - key: DB_HOST
        sync: false # e.g. aws-0-ap-southeast-1.pooler.supabase.com
      - key: DB_PORT
        value: "6543"
      - key: DB_NAME
        value: "postgres"
      - key: DB_USERNAME
        sync: false # e.g. postgres.xxxxxxxxxxxxxxxxxxxx
      - key: DB_PASSWORD
        sync: false
      - key: DB_SSL_MODE
        value: "require"

      # ── Auth ──────────────────────────────────────────────────────────────
      - key: JWT_SECRET
        generateValue: true

      # ── Media storage ─────────────────────────────────────────────────────
      - key: STORAGE_PROVIDER
        value: "cloudinary"
      - key: CLOUDINARY_CLOUD_NAME
        sync: false
      - key: CLOUDINARY_API_KEY
        sync: false
      - key: CLOUDINARY_API_SECRET
        sync: false

      # ── App config ────────────────────────────────────────────────────────
      - key: SUPPORTED_LOCALES
        value: "en,vi"
      - key: CONTENT_TYPES_DIR
        value: "content-types"

  # ── React Frontend (Static Site, free tier) ─────────────────────────────────
  - type: web
    name: cms-web
    runtime: static
    plan: free
    buildCommand: cd apps/web && bun install --frozen-lockfile && bun run build
    staticPublishPath: apps/web/dist
    headers:
      - path: /*
        name: X-Frame-Options
        value: DENY
    routes:
      - type: rewrite
        source: /api/*
        destination: https://cms-api.onrender.com/api/*
      - type: rewrite
        source: /auth/*
        destination: https://cms-api.onrender.com/auth/*
      - type: rewrite
        source: /graphql
        destination: https://cms-api.onrender.com/graphql
      - type: rewrite
        source: /health
        destination: https://cms-api.onrender.com/health
      - type: rewrite
        source: /*
        destination: /index.html
