# Archive — Phase M: Media + Schema-Driven Forms

> All tasks completed. Archived from `tasks/todo.md`.

---

## Phase M — Media + Schema-Driven Forms

- [x] M1 `FieldDefinition` entity + `Fields` on `ContentType` (BE entity + usecase validation + sync)
- [x] M2 `MediaAsset` hash/filename fields + `List` endpoint + `MediaLibrary` modal component + `MediaInput` rewrite
- [x] M3 `ContentTypeBuilder` component + `renderSchemaField` utility
- [x] M4 `CollectionDetailPanel` uses `renderSchemaField` (schema-driven form fields)
- [x] M5 `MediaLibraryPage` + Sidebar Settings group + `/admin/settings/media` route
- [x] ✅ Checkpoint M: schema-driven forms render from `Fields`; MediaLibrary modal opens from MediaInput; `/admin/settings/media` accessible from sidebar
